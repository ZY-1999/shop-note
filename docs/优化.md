### 管理页面

1. item 间增加一定gap 同时可适当缩小 item 的 padding
2. 新增员工 电话&备注 标记‘可选’
3. ‘作废’字样‘ 改为 ‘删除’
4. 商品 分类 & 编码 删除；增加标签栏（可选）
5. 表单item 改为 label: input 形式

6. 表单 label: input 应保持在一行
7. 员工栏 电话未输入时展示 ‘--’
8. 员工/商品 item需可点击，点击进入编辑表单

### 记账页面

1. 每个员工展示信息中， 2、3 行的 库存和金额合并为一行 格式 ‘库存：m件/n种 金额’
2. ‘出库’ 改为 ‘出单’
3. 出/入单 商品选择改为 label 形式，点击后选中某个商品&数量x1，每点击一次＋1, 商品数量应使用数字input 可通过点击进行增减
4. 日期使用 YYYY/MM/DD 格式
5. 某个员工存在商品库存
6. 备注改为单行 label: input 形式
7. 时间需突出可选效果

### 员工详情

1. ‘持仓’改为‘库存’ 同时显示总金额，并提供是否显示详情点击图标，商品详情默认不展开
2. 记录栏 显示共多少条 入库金额 出库金额
3. 记录按时间（天）倒序展示，需展示时间分割线，需支持分批获取，样式类似
   2026/07/10 入库金额 出库金额 --------
   入库金额 xxxxx  
   出单金额 xxxxx  
   2026/07/09 入库金额 出库金额 --------

### 汇总页面

1. 整体重构，抛弃现有页面 首先是一个时间段选择，支持快捷选择本月/上月/本周/上周
2. 内容组成
   库存 总金额 详情图标（点击展开按商品展示的详情）

记录栏 入库金额 出库金额

记录按时间（天）倒序展示，需展示时间分割线，需支持分批获取，样式类似
2026/07/10 入库金额 出库金额 --------
员工A 入库金额 出单金额 点击可显示详情
员工B 入库金额 出单金额 点击可显示详情

2026/07/09 入库金额 出库金额 --------

### 修改

1. 分离会员 入库出库功能，会员不再拥有入库功能，库存统一由管理员补充；
2. 管理页新增一个‘补货’tab 使用管理员补货 staff_id 记为 ‘-1’ ;
3. 全局仅存在一份库存，会员出库从全局库存扣除；
4. 管理页面新增一个配置tab, 目前支持配置单价 ，单价需用于会员出库过程的金额计算，出库金额统计细分为 多少单 多少零售金额 出库金额 / 单价 取整等于单数，余数为零售金额，不足1单全记零售
5. 会员只需要充值 & 出库 功能，会员充值仅需金额；出库时从全局库存扣库存&从余额扣钱
6. 出库记录应记录当前的单价，避免歧义

### 出库样式优化

1. 商品label 容器上下加一点gap
2. 商品label 点击后不要在label 内动态展示数量，商品label只有选择的作用
3. 选中后商品展示优化：
   a. 多个商品选中后上下加一点gap
   b. 单个商品改为左右布局 左边两行：标题第1行，第2行展示价格 + 当前库存 ； 右侧展示当前数字input组件
4. 进入页面后需立即请求一次当前商品，避免和可选商品、库存不一致
5. ‘提交’按钮改为绿色，并确认保存正确
6. 表单项 'label input' 统一不加冒号
